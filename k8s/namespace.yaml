apiVersion: v1
kind: Namespace
metadata:
  name: taskflow
  # Namespace = logical isolation boundary within a cluster.
  # All taskflow resources live here, separated from other apps on the same cluster.
